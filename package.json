{
  "name": "dajeong",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": "24.19.x",
    "pnpm": "11.22.0"
  },
  "scripts": {
    "check:toolchain": "node scripts/doctor.mjs",
    "check:shared-config": "pnpm --filter @dajeong/config --filter @dajeong/design-tokens --filter @dajeong/web --filter @dajeong/mobile --if-present run lint && pnpm --filter @dajeong/design-tokens --filter @dajeong/web --filter @dajeong/mobile run typecheck && pnpm verify:typecheck-failure",
    "verify:typecheck-failure": "node scripts/verify-typecheck-failure.mjs",
    "workspace:list": "turbo ls"
  },
  "devDependencies": {
    "turbo": "2.10.10",
    "typescript": "6.0.3"
  }
}
